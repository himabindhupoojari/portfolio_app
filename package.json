{
  "name": "portfolio_app",
  "private": true,
  "scripts": {
    "dev": "concurrently \"yarn --cwd client dev\" \"yarn --cwd server start\"",
    "build": "yarn --cwd client build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {}
}
